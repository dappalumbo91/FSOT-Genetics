// Generated catalog milliscale. Pin D1D38A.
#[allow(clippy::unreadable_literal)]
pub fn check() {
    assert_eq!(0u32, 0u32); // free_parameters_zero
    assert_eq!(1618u32, 1618u32); // phi_milli
    assert_eq!(382u32, 382u32); // leftover_floor_milli
    assert_eq!(618u32, 618u32); // close_homolog_milli
    assert_eq!(8u32, 8u32); // chem_backbone_D
    assert_eq!(7u32, 7u32); // chem_disulfide_D
    assert_eq!(9u32, 9u32); // chem_salt_D
    assert_eq!(14u32, 14u32); // chem_pack_D
    assert_eq!(8u32, 8u32); // chem_hbond_D
    assert_eq!(9u32, 9u32); // chem_molecular_D
    assert_eq!(13u32, 13u32); // chem_tertiary_D
    assert_eq!(7u32, 7u32); // long_range_gate
    assert_eq!(7u32, 7u32); // chem_link_card
    assert_eq!(10u32, 10u32); // product_n
    assert_eq!(10u32, 10u32); // product_sub2A
    assert_eq!(133u32, 133u32); // product_median_milliA
    assert_eq!(471u32, 471u32); // alphafold_median_milliA
    assert!(133u32 < 471u32); // product_lt_alphafold
    assert!(133u32 < 13574u32); // product_lt_bulk
    assert_eq!(26u32, 26u32); // homolog_measured
    assert_eq!(26u32, 26u32); // homolog_folded_eq_measured
    assert_eq!(1u32, 1u32); // homolog_true_miss
    assert!(10u32 <= 24u32); // homolog_close_count
    assert_eq!(2u32, 2u32); // analog_jobs
    assert!(6u32 < 107402u32); // male_vnc_gt_olf
    assert!(6u32 < 77729u32); // male_jo_gt_olf
    assert!(8u32 < 101619u32); // banc_vnc_gt_olf
    assert!(8u32 < 66369u32); // banc_jo_gt_olf
    assert!(4292u32 < 166016u32); // larva_mech_gt_olf
    assert!(745u32 < 77442u32); // worm_male_sex_gt_herm
    assert_eq!(6u32, 6u32); // plant_arabidopsis_folded
    assert_eq!(12u32, 12u32); // plant_rice_maize_folded
    assert_eq!(0u32, 0u32); // plant_rice_maize_miss
    assert!(618u32 <= 741u32); // plant_min_id_close
    assert!(0u32 < 453u32); // worm_cells
    assert!(0u32 < 205u32); // ciona_cells
    assert!(0u32 < 1720u32); // platynereis_cells
    assert!(100000u32 < 165122u32); // male_neurons
    assert!(100000u32 < 175401u32); // banc_neurons
    assert_eq!(0u32, 0u32); // ciona_unsigned_gaba
    assert_eq!(0u32, 0u32); // platynereis_unsigned_gaba
}
